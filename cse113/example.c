//remember for char in, int num, and fgets(in, 32, stdin), num = atoi(in); after fgets remeber to #include <stdlib.h>
//atof convert string to double.
//atoi convert string to int
//sscanf() can only read from string. not user input. can check if format matches.
//
