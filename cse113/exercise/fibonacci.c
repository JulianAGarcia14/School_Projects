#include <stdio.h>
#include <stlib.h>

unsigned long fib(int m)
{
	if (m == 0) {
		return 0;
	} else if (m == 1) {
		return 1;
	} else {
		return
	}
}


unsigned long gcd(int p, int q)
{

	return (r = p % q) == 0 ? q : gcd(q, r);
}


int main(void)
{






}
